$page.setTitle("UserAttributes")
$page.setLinkColor($ui.alink)
$page.setVlinkColor($ui.vlink)
<script type="text/javascript">
    window.currentID = "$!om.getId()";
        #if($om.getProject())
            #if($om.getProject().equals(""))
            window.currentProject = "$!project";
            #else
            window.currentProject = "$!om.getProject()";
            #end
        #else
        window.currentProject = "$!project";
        #end

    window.currentSubject = "$!om.getSubjectId()";
    window.currentLabel = "$!om.getLabel()";
</script>
#if ($data.message)
<div class="error">$data.message</div>
#end
<form ID="userAttributesAcceptanceForm" name="userAttributesAcceptanceForm" method="post" action="$link.setAction("AcceptTerms")">
    <input type="hidden" name="project" value="$project.getId()"/>
    <input type="hidden" name="project" value="$!{project}">
    <input type="hidden" name="part_id" value="$!{part_id}">
#*
    <input type='hidden' name='biomedia:userAttributes/ID' value='$!item.getProperty("ID")'/>
*#
    <input type="hidden" name="biomedia:userAttributes/project" value='$!{project}'/>
#*    <input type='hidden' name='biomedia:userAttributes/subject_ID'
           value='$!item.getProperty("biomedia:userAttributes/subject_ID")'/>
    <input type='hidden' name='biomedia:userAttributes/label' value='$!item.getProperty("biomedia:userAttributes/label")'/>*#
    <input type="hidden" name="biomedia:userAttributes.date.month" id="biomedia:userAttributes.date.month" value=""/>
    <input type="hidden" name="biomedia:userAttributes.date.date" id="biomedia:userAttributes.date.date" value=""/>
    <input type="hidden" name="biomedia:userAttributes.date.year" id="biomedia:userAttributes.date.year" value=""/>
    <input type="hidden" name="$om.getXSIType()/ID" id="$om.getXSIType()/ID" value="$!om.getId()"/>

    #if($vr)
        <div class="error">Invalid parameters:<BR>$vr.toHTML()</div>
        <HR>
    #end

    <table>
        <tr>
            <td><h3>dHCP $!{project.getName()} data set: Terms of Use</h3>
            </td>
        </tr>
        <tr>
            <td>&nbsp;
            </td>
        </tr>
        <tr>
            <td>Please accept the attributes and conditions in order to be able to download data.
            </td>
        </tr>
        <tr>
            <td>&nbsp;
            </td>
        </tr>

    </table>

</form>

<script type="text/javascript">
    function validateForm() {
        var today = new Date();
        document.getElementById('biomedia:userAttributes.date.month').value = today.getMonth();
        document.getElementById('biomedia:userAttributes.date.date').value = today.getDate();
        document.getElementById('biomedia:userAttributes.date.year').value = today.getFullYear();

        document.userAttributesAcceptanceForm.submit();
        return true;
    }
</script>

